function brl(v){return new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v)}
function calcularOrcamento(){const r=Number(document.getElementById('renda').value),g=Number(document.getElementById('gastos').value);const el=document.getElementById('resultadoOrcamento');if(r<=0){el.textContent='Informe uma renda mensal válida.';return}const saldo=r-g;el.textContent=`Saldo mensal: ${brl(saldo)} — comprometimento da renda: ${(g/r*100).toFixed(1)}%.`}
function calcularJuros(){const p=Number(document.getElementById('aporte').value),i=Number(document.getElementById('taxa').value)/100,n=Number(document.getElementById('meses').value);const el=document.getElementById('resultadoJuros');if(p<=0||n<=0||i<0){el.textContent='Informe valores válidos.';return}const total=i===0?p*n:p*((Math.pow(1+i,n)-1)/i);el.textContent=`Valor estimado: ${brl(total)}. Cálculo ilustrativo, sem considerar impostos, taxas ou inflação.`}
document.getElementById('ano').textContent=new Date().getFullYear();
