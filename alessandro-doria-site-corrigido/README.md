# Alessandro Dória — Consultor Financeiro

Site estático em HTML, CSS e JavaScript puro.

## Deploy no Cloudflare Pages via GitHub
- Branch: `main`
- Diretório raiz: `/`
- Comando de build: deixar vazio
- Comando de implantação: deixar vazio
- O arquivo `index.html` está na raiz do repositório.

Não há Node, npm, framework ou etapa de build necessária.
